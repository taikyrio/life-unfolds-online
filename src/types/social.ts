

export interface SocialMediaAccount {
  platform: string;
  followers: number;
  verified: boolean;
}

export interface RealEstateProperty {
  id: string;
  type: string;
  value: number;
  location: string;
}

